package com.f1pulse.app.ui.theme

import androidx.compose.ui.text.font.FontFamily

/** Portable font aliases used by the public source distribution. */
val Formula1Family = FontFamily.SansSerif

/** Monospaced system family for large numeric metrics. */
val Formula1WideFamily = FontFamily.Monospace

/** System cursive fallback for decorative names. */
val NorthwellCleanAltFamily = FontFamily.Cursive
